[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "opencomputer-agents-sdk"
version = "0.1.0"
description = "Official Python SDK for OC Agents - Run AI agents programmatically"
readme = "README.md"
license = "MIT"
authors = [
    { name = "OpenComputer", email = "hello@opencomputer.dev" }
]
keywords = ["opencomputer", "ai", "agents", "sdk", "automation"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
requires-python = ">=3.9"
dependencies = [
    "websockets>=12.0",
    "httpx>=0.25.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
    "mypy>=1.0",
    "ruff>=0.1.0",
]

[project.urls]
Homepage = "https://opencomputer.dev"
Documentation = "https://opencomputer.dev/docs/sdk"
Repository = "https://github.com/opencomputer/agents-sdk"
Issues = "https://github.com/opencomputer/agents-sdk/issues"

[tool.hatch.build.targets.wheel]
packages = ["oc_agents"]

[tool.mypy]
python_version = "3.9"
strict = true
warn_return_any = true
warn_unused_configs = true

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
